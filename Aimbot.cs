using System.Reflection;
using System.IO;
using System.Collections.Generic;
using System.Runtime.InteropServices;
using UnityEngine;

// Be sure to use .NET Framework 4.0
namespace UnityAimbot
{
    // Monobehavior is the base class from which every script is derived
    // https://docs.unity3d.com/ScriptReference/MonoBehaviour.html
    public class Main : MonoBehaviour
    {
        // Called once before when the script is activated 
        public void Start()
        {
        }
        // Called every frame
        public void Update()
        {
        }
        // Used for rendering and handling GUI events
        // If you want to add an ESP, draw lines, or add a GUI for additional hacks it can be done here
        public void OnGUI()
        {
        }
    }
}